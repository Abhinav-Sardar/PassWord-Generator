import React from 'react'
// import ModalComponent from
const Modal = ({modalContent}) => {
    return (
        <div>
            <center><h1>{modalContent}</h1></center>
        </div>
    )
}

export default Modal ; 
